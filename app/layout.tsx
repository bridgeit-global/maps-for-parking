import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Maps for Parking - Mumbai",
  description: "Empowering Mumbaikars to navigate parking rules and avoid unnecessary penalties. Explore interactive parking zones across Mumbai.",
  openGraph: {
    title: "Maps for Parking - Mumbai",
    description: "Empowering Mumbaikars to navigate parking rules and avoid unnecessary penalties",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
